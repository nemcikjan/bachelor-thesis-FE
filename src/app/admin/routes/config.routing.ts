import { Routes } from '@angular/router';
import { ConfigComponent } from '../config/config.component';

export const ConfigRouting: Routes = [
  {
    path: 'config',
    component: ConfigComponent
  }
];
